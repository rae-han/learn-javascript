// import { apiStatus: { PENDING, SUCCESS, RUNNING } } from './global';
const { apiStatus: { PENDING, SUCCESS, RUNNING } } = require('./global');

console.log(PENDING, SUCCESS, RUNNING); // Symbol(PENDING) Symbol(SUCCESS) Symbol(RUNNING)